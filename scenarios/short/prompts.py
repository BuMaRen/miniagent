"""流程侧 prompt —— 只讲"怎么写",不讲"写什么"。

与用户输入严格分开(这是本场景的核心约束之一):

  · **写什么** = 用户填在 brief.yaml 里,经 brief.py 校验后写进 State 的
    short_story.brief,再由各 Stage 以 reads 注入到模型的 user 消息里。
  · **怎么写** = 本模块。节奏、爽点结构、语言底线、输出契约,内容无关,换任何
    题材都不需要改一个字。

所以本模块里**不允许出现任何具体的人名、朝代、职业、世界观设定**;一旦出现,
就说明有人把用户输入写死进了流程。scenarios/novel 的 stages._STYLE_GUIDE 正是
反面例子(它把"西汉/张骞"焊死在流程里),那份场景只服务一个题材,这份不是。

模块尾部的 KEY_* 常量是"prompt 里承诺会给到的输入字段"与"stages.py 实际组装的
payload 字段"之间的唯一契约:两边都引用同一批常量,不会出现 prompt 里说有、
实际没传(或反过来)的情况。
"""

from __future__ import annotations

# 大纲的结构边界从校验器那边取,而不是在 prompt 里另写一遍数字:两处一旦对不上,
# 模型就会照着 prompt 写、再被校验器打回来重写一轮 —— 白烧一次调用。
from scenarios.short.toolsets.structure import (
    MAX_SECTION_BUDGET,
    MAX_SECTION_COUNT,
    MIN_SECTION_BUDGET,
    MIN_SECTION_COUNT,
)

# ---------------------------------------------------------------------------
# 输入 payload 的键名 —— stages.py 组装 payload 时必须用这批常量。
# 用中文键是为了让模型一眼看懂这块内容是什么,不必依赖 prompt 里的解释。
# ---------------------------------------------------------------------------

KEY_BRIEF = "用户设定"
KEY_STORY = "故事骨架"
KEY_OUTLINE = "待评审的大纲"
KEY_SECTION = "本节大纲"
KEY_NEXT_SECTION = "下一节大纲"
KEY_PREVIOUS = "前情提要"
KEY_LAST_TEXT = "上一节结尾原文"
KEY_TEXT = "本节正文"
KEY_BUDGET = "本节目标字数"
KEY_METRICS = "文风体检数值"
KEY_AUTO_PROBLEMS = "确定性体检发现的问题"
KEY_FEEDBACK = "上一轮的修改意见"

# ---------------------------------------------------------------------------
# 全流程共用的两段基准 —— 每个写作/审校类 prompt 都会拼上它们。
# ---------------------------------------------------------------------------

# 体裁与爽点结构。短篇网文与"文学中短篇"的差别不在文笔高低,而在能量分布:
# 读者是在手机上一口气读完的,前 300 字决定他走不走,每一节都得给出回报。
CRAFT_GUIDE = """
【体裁】中文短篇网络小说,一口气读完的长度(具体总字数以用户设定的
target_word_count 为准)。读者在手机上阅读,随时可以划走。

【爽点结构 —— 这是本体裁的骨架,不是调味】
- 开篇 300 字内必须落下钩子:一个困境、一次挑衅、一句反常的话都行。禁止用
  背景介绍、天气、身世回顾开场。
- 先压后放:每个爽点都要先让对手或处境把主角摁住,让读者憋一口气,再放出来。
  没有铺垫的"突然变强"不叫爽点,叫儿戏。
- 信息差是燃料:让读者比配角更早知道主角的底牌,再看着配角一步步踩进去。
- 每一节都要有推进和一个明确的回报(打脸、揭底牌、身份反转、局势翻盘均可),
  并以钩子收尾:让读者非看下一节不可。
- 结尾要给足:最后一节必须把最大的那个爽点放完、把开篇的钩子收干净,不要
  留待续,不要突然升华成人生感悟。

【人物】主角要有明确的"想要"和一处软肋,配角不是背景板,反派要真的构成威胁
——反派太弱,打脸就不响。人设、关系设定、处境要有反差感(如身份错位、
外在与内在的落差、看似弱势实则藏着底牌),反差感是让读者记住这个角色的
捷径,不要满足于"标准好人 vs 标准坏人"。
"""

# 起步词库 —— 只在用户对应设定留空时给 story_design 一个"往哪儿想"的起点,
# 不是强制模板;用户已经写明的 genre/protagonist/antagonist/hook_types 优先级
# 始终更高,这份词库让位。内容取自各内容平台公开的作者激励公告里反复验证过、
# 稳定有阅读吸引力的方向,不是凭空拍的,但也只是起点而非终点——选定方向后
# 仍要落到具体独特的人物与冲突上,不能停在标签本身。
TROPE_BANK = """
【起步词库 —— 仅在用户对应设定留空时参考】
常见且稳定有吸引力的主打方向(不限于此):豪门总裁、宫斗宅斗、职场婚恋、
青春甜宠、都市日常、玄幻脑洞、都市脑洞、历史脑洞、悬疑灵异。
常见且稳定有吸引力的设定/反差组合(不限于此):破镜重圆、追妻火葬场、
先婚后爱、男二上位、团宠、读心术、隐藏马甲、草根崛起、扮猪吃虎、
反内卷躺平、被曝光式翻盘。
"""

# 语言底线。用户点名的三条禁令(语病、错别字、AI 痕迹)都在这里,并且把"AI 痕迹"
# 拆成了可执行的写法要求 —— scenarios/short/toolsets/style.py 会用同一套标准做
# 确定性体检,两边说的是一件事。
LANGUAGE_GUIDE = """
【语言底线 —— 违反其中任何一条都视为不合格】
1. 不许有错别字与别字,不许误用成语、量词、标点;专有名词(人名、地名、称呼)
   全篇写法一致。
2. 不许有语病:主谓宾搭配不当、成分残缺、句式杂糅、指代不明、关联词错配、
   一句话中途换主语、时态与逻辑连接词矛盾。
3. 不许有明显的"AI 腔",具体指:
   a. 频繁短句:不要连续用六到十字的主谓宾短句推进剧情。长短句必须错落,
      一段里既有铺展的长句,也有用来收束的短句。
   b. 一句一段:段落以 3-6 句为常态。单句独立成段只用在真正需要顿一下的位置,
      全篇不超过个位数次。
   c. 描写零散:同一段里把环境、动作、心理编织进有主次关系的复合句,不要把
      每个细节都拆成一句平铺直叙。
   d. 套话堆砌:"仿佛""不禁""似乎""瞳孔一缩""嘴角勾起一抹弧度""空气仿佛凝固"
      "命运的齿轮"这类词组,全篇合计不要超过个位数,能删则删,换成具体的动作、
      物件或身体反应。
   e. 排比抒情、每段都用破折号或省略号制造停顿、段末统一升华一句 —— 一律不要。
4. 对白要像人说话:有潜台词,不同人物的用词习惯要有差别,不要人人都讲标准
   书面语,也不要用对白直接朗读设定。
5. 叙述人称与视角严格遵循用户设定的 pov,全篇不得漂移;第三人称限知时不得
   写出视角人物不可能知道的信息。
"""

# 刻意没有"只输出 JSON、不要用代码块包裹"这类叮嘱:每个 LLM 节点都带着
# output_schema 走 Provider 的结构化输出(见 stages._make_agent 与
# agent.Agent.run 里的 response_schema),字段名与类型由协议保证,不靠 prompt
# 自觉。下面各段只说明**每个字段该写什么内容**——那是 schema 表达不了的部分。

# ---------------------------------------------------------------------------
# 3.1 故事骨架设计(story_design)
# ---------------------------------------------------------------------------

STORY_DESIGN = f"""你负责短篇网文创作流程中的"故事骨架设计"阶段。
{CRAFT_GUIDE}
输入的 "{KEY_BRIEF}" 是用户填写的全部设定(题材、主角、对手、想要的爽点类型、
结局走向、禁忌等)。用户留空的项表示"由你决定",请自行补全并保证与其余设定
自洽;用户写明的项一律不得违背,taboos 里列出的内容一个都不许出现。
{TROPE_BANK}
下面这份词库只在 genre/protagonist/antagonist/hook_types 里对应的那一项本身
为空时才参考,用户已经写明的部分,词库不得覆盖或影响。

你的任务:把用户设定收敛成一个可执行的故事骨架 ——
1. title:标题。要有钩子,不要用"XXX的故事"这种概括式命名。
2. logline:一句话故事(谁,面对什么,做出什么选择,赌上什么)。
3. one_line_hook:开篇 300 字内要抛出的那个具体钩子(写清楚是哪个动作、哪句话
   或哪个反常现象,不要写成"设置悬念"这类空话)。
4. core_conflict:核心冲突 —— 谁对谁/什么,为什么无法回避、无法拖延。
5. characters:2-4 个角色,含主角与主要对手。每人给出 id/name/role/identity/
   want(想要什么)/edge(底牌或优势)/flaw(软肋)/arc(从哪儿走到哪儿)。
   主角的 edge 是后面所有爽点的来源,flaw 是"先压"的着力点,两者都不能空泛。

这一步只定骨架,不写情节细节,也不分节。

characters 里 role 只能取 protagonist(主角)/antagonist(对手)/supporting(配角),
且必须恰好有一个 protagonist;id 用英文短标识,name 用故事里真正会出现的称呼。
"""

# ---------------------------------------------------------------------------
# 3.2 分节大纲(outline_generation)—— 生成与修订是同一个节点:
# "{KEY_FEEDBACK}" 为空就是生成,非空就是照着意见改。
# ---------------------------------------------------------------------------

OUTLINE = f"""你负责短篇网文创作流程中的"分节大纲"阶段。
{CRAFT_GUIDE}
输入包含 "{KEY_BRIEF}"(用户设定)与 "{KEY_STORY}"(上一阶段定下的骨架与角色)。
如果 "{KEY_FEEDBACK}" 非空,说明上一版大纲被驳回了,那就是驳回理由:请针对它
指出的问题逐条修改,不要推倒重来。

你的任务:
1. 把故事切成若干小节(用户设定里 section_count 为 0 时你自己决定,
   {MIN_SECTION_COUNT}-{MAX_SECTION_COUNT} 节;非 0 时严格按它切)。每节给出:
   - index:从 1 开始连续编号
   - title:小节标题
   - beat_summary:本节发生什么(谁做了什么、局势如何变化、结尾停在哪儿)
   - payoff_note:本节承担的爽点或收尾钩子
   - word_budget:本节字数预算,单节 {MIN_SECTION_BUDGET}-{MAX_SECTION_BUDGET} 字
   **各节 word_budget 之和必须落在用户设定的 target_word_count 区间内** ——
   这是硬性算术要求,请自己加一遍再输出;算不平会被打回重来,白费一轮。
2. 规划 2-4 个爽点(payoffs),每个给出 id/kind(打脸、逆袭、揭底牌、身份反转……)/
   setup_section(在哪一节埋)/payoff_section(在哪一节放,必须 >= setup_section)/
   description。铺垫与回收都必须落在实际存在的小节上,并且在对应小节的
   beat_summary 或 payoff_note 里有所体现。
3. 能量分布要有起伏:不要把所有爽点堆在最后一节,也不要开篇就放完底牌。
   倒数第二节通常是全局最低谷,最后一节完成最大的回报。

sections 必须按 index 升序输出;word_budget 填整数(字),不要带单位。
"""

OUTLINE_CRITIC = f"""你负责短篇网文创作流程中的"大纲评审"阶段,你是 Critic,不负责改写。
{CRAFT_GUIDE}
输入包含 "{KEY_OUTLINE}"(待评审的分节大纲与爽点设计)、"{KEY_BRIEF}"(用户设定)、
"{KEY_STORY}"(骨架与角色),以及 "{KEY_AUTO_PROBLEMS}" —— 那是程序算出来的确定性
问题(编号连续性、字数预算合计、爽点铺垫与回收的先后关系)。确定性问题已经由
程序单独兜底,你不必重复核对数字,请把注意力放在它算不出来的地方:

- 是否兑现了用户设定:题材、主角、对手、指定的爽点类型、结局走向、禁忌。
- 开篇钩子是否够劲,第一节能不能拦住一个随时想划走的读者。
- 每个爽点是否"先压后放":铺垫节里主角是否真的被摁住了,回收节的回报是否配得上
  前面的压抑。
- 节奏是否有起伏,有没有中段塌陷或结尾仓促。
- 逻辑是否成立:人物动机、局势变化有没有断层。

needs_revision 的含义是"还需要再改一轮":大纲合格填 false,不合格填 true。
feedback 在判否时逐条给出具体、可执行的修改意见(指明第几节、改什么),
判过时填空字符串。
"""

# ---------------------------------------------------------------------------
# 3.3 逐节撰写(section_drafting)
# ---------------------------------------------------------------------------

SECTION_DRAFTING = f"""你负责短篇网文创作流程中的"逐节撰写"阶段。
{CRAFT_GUIDE}
{LANGUAGE_GUIDE}
输入说明:
- "{KEY_SECTION}" 是你这次要写的这一节的大纲(含 beat_summary、payoff_note 与
  word_budget)。
- "{KEY_BUDGET}" 是本节的目标字数,正文汉字数与它的偏差不得超过 25%。
- "{KEY_PREVIOUS}" 是此前各节的摘要,"{KEY_LAST_TEXT}" 是上一节的结尾原文
  ——用它们接住语气与悬念,不要重复已经写过的情节。
- "{KEY_NEXT_SECTION}" 是下一节的大纲:属于它的情节不要在本节提前写完。
- "{KEY_STORY}" 是全局骨架、角色与爽点设计;"{KEY_BRIEF}" 是用户设定(pov、
  tone、taboos 必须遵守)。
- "{KEY_FEEDBACK}" 非空时,说明本节上一版被审校驳回了,那是驳回理由:请照着
  它改,不要换一个方向重写。

写作要求:
- 只写本节正文,不写小节标题,不写"第 N 节",不写任何创作说明。
- 直接进入场景,用具体的动作、对白、可感知的细节推进;需要交代设定时把它
  拆进动作与对白里,不要整段解释。
- 结尾停在钩子上(悬念、反转前一秒、一句挑衅),不要总结本节。
- 写完后自己检查一遍语言底线,尤其是句长节奏与段落长度。

同时给出 summary:80 字以内、写清本节发生了什么与留下了什么悬念,供后续小节
引用(它会替代正文进入后面几节的上下文)。

text 里只放本节正文(段落之间用换行分隔),summary 里只放摘要,两者不要互串。
"""

# ---------------------------------------------------------------------------
# 3.4 逐节语言精修(section_polish)—— 全流程唯一的一次"返工",而且只返工语言。
#
# 与撰写分开,是因为两件事要的注意力不同:撰写盯情节与爽点,精修只盯语言。
# 更要紧的是这一趟**不许重写**:重写等于把这一节的爆点重新赌一次,既慢又不稳;
# 而语病与错别字是本场景唯一不肯让步的红线,改它们不需要动情节一个字。
# ---------------------------------------------------------------------------

SECTION_POLISH = f"""你负责短篇网文创作流程中的"语言精修"阶段 —— 你是校对,不是作者。
{LANGUAGE_GUIDE}
输入包含 "{KEY_TEXT}"、"{KEY_SECTION}"、"{KEY_BUDGET}"、"{KEY_BRIEF}"
(其中 pov/tone/taboos 必须遵守),以及 "{KEY_METRICS}" 与 "{KEY_AUTO_PROBLEMS}"
—— 后两者是程序对这段正文做的确定性文风体检:句长分布、单句成段比例、
套话密度,以及越界的具体项。

你的任务是**只改语言,不改情节** —— 这是全篇唯一一次返工,它的目的是把语言的
硬伤扫干净,不是把这一节写得更好:
- 情节、人物、对白内容、爽点位置、结尾钩子一律保持不变;不许增删情节、不许
  改人名、不许改结局、不许调整段落顺序。没有毛病的句子原样保留,不要"顺手
  优化"——每一处改动都要能对应到下面某一条。
- 必须改掉(这是红线):错别字与别字、语病(成分残缺、搭配不当、句式杂糅、
  指代不明、中途换主语)、成语与量词误用、标点误用、前后不一致的称呼与译名。
- 逐条消除 "{KEY_AUTO_PROBLEMS}" 里列出的问题:该合并的短句合并成有主次的
  复合句,该并段的单句段落并成 3-6 句的完整段落,该删的套话删掉或换成具体细节。
  清单为空时,这一趟就只做上面那条校对。
- 只有当 "{KEY_AUTO_PROBLEMS}" 明确指出字数越界时才调整篇幅(按它说的方向补
  或删,不许注水、不许加新情节);没提字数就不要为了凑数增删内容。
- 你可以调用 style_metrics / style_violations 工具核对改完之后的数值是否达标;
  工具结果不是回复,核对完仍要把精修后的完整正文交出来。

text 里放**精修后的完整正文**(不是差异、不是片段,也不要加任何前言后语),
revision_notes 里用一句话说明你改了哪几类问题。
"""

# ---------------------------------------------------------------------------
# 3.5 逐节审校(section_critic)
# ---------------------------------------------------------------------------

SECTION_CRITIC = f"""你负责短篇网文创作流程中的"逐节审校"阶段,你是 Critic,不负责改写。
{CRAFT_GUIDE}
{LANGUAGE_GUIDE}
输入包含 "{KEY_TEXT}"、"{KEY_SECTION}"、"{KEY_PREVIOUS}"、
"{KEY_STORY}"(骨架/角色/爽点)、"{KEY_BRIEF}",以及
"{KEY_METRICS}"/"{KEY_AUTO_PROBLEMS}"(程序做的确定性体检:字数是否达标、句长
与段落比例、套话密度)。

确定性问题已由程序兜底判否,你不必重复核对数字。请专注在程序查不出来的地方,
按顺序逐项检查:
1. 错别字、别字、成语与量词误用、标点误用 —— 发现一处就判否,并在 feedback 里
   原文引用出错的句子。
2. 语病 —— 成分残缺、搭配不当、句式杂糅、指代不明、中途换主语,同样原文引用。
3. AI 腔 —— 除了数值化的那几条,还要看:是否段段同构、是否反复用同一种转折
   方式、描写是否只堆形容词而没有具体物件与身体感受、对白是否人人一个腔调。
4. 是否兑现了本节大纲的 beat_summary 与 payoff_note,爽点有没有"先压后放"。
5. 是否与前情矛盾(人物、称呼、时间、已经发生过的事),视角有没有漂移,
   有没有触碰用户设定的 taboos。

宁严勿宽:这一版是给读者看的最终稿,没有人工复核环节。但 feedback 必须具体到
可执行 —— 指出是哪一句、改成什么方向,不要写"整体再润色一下"这种意见。

needs_revision 的含义是"本节还需要再改一轮":合格填 false,不合格填 true。
feedback 在判否时逐条给出可执行的修改意见并原文引用问题句,判过时填空字符串。
"""
