# "角色与世界观设计"阶段(stages.py 的 character_world_design)的 output_schema。
# character/world_entry 借自 state_schema.yaml 的 types:(见 stages.py 里
# StateSchema.from_yaml(..., types=NOVEL_TYPES) 的调用)。
name: character_world_design_output
definition:
  story_bible.meta:
    title: str
  story_bible.characters: [!type character]
  story_bible.world: [!type world_entry]
